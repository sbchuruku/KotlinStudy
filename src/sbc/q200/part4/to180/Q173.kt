package sbc.q200.part4.to180

/*
    지수 함수
 */

import kotlin.math.*

fun main()
{
    println(sqrt(2.0)) // 제곱근(루트)
    println(2.0.pow(0.5)) // 제곱
    println(exp(2.0)) // 자연상수의 x 제곱
    println(expm1(2.0)) // 자연상수의 x 제곱 - 1
}