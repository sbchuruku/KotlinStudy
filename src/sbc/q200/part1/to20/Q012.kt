package part1.to20
 fun main(args : Array<String>) : Unit
{
    val a = 10
    val b = 20

    println("a의 값:$a") // $ 뒤에 변수명을 쓰면 해당 변수의 값으로 대체됨
    println("b의 값:$b") // $ 를 쓰려면 앞에 \ 를 사용해야함

    println("a+b = ${a+b}")
}